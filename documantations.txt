1. uncomment django superuser settings in ./compose/local/django/start
2. Got to ./frontend, run "npm install"
3. Run "npm run build" to build dist folder
4. go to the dockerfile with "cd .." and run "docker compose -f local.yml up -d --build"
5. Got to ./frontend, run "npm run serve"
